# 💡 VS Code ポモドーロ運用 Tips

本システムをより高速・快適に運用するためのテクニック集です。

---

## 1. 高速編集テクニック
エディタの機能を活用し、入力のオーバーヘッドを最小化します。

### ⌨️ 実績の一括入力 (マルチカーソル)
- **操作**: 予定の `p` を選択し、`Ctrl + D` (Mac: `Cmd + D`) を数回押す。
- **効果**: 同一・近傍行の `p` を複数選択状態にし、一括で大文字の `P` に書き換えられます。

### 🚀 タスクの昇格・並べ替え (行移動)
- **操作**: 移動させたいタスク行にカーソルを置き、`Alt + ↑ / ↓` (Mac: `Option + ↑ / ↓`)。
- **効果**: `Weekly` から `Daily` セクションへ移動させる際など、カット＆ペースト不要で行を物理的に移動できます。

### ✅ チェックボックスの高速切り替え
- **操作**: `Alt + C` (要: 拡張機能 Markdown All in One)
- **効果**: `[ ]` と `[x]` を一瞬で切り替えます。

---

## 2. メンテナンスの自動化 (正規表現)
週末の棚卸し時に、完了済みタスクを一掃するためのパターンです。

### 🧹 完了タスクの一括削除
- **場所**: `Stock.md` 内で `Ctrl + H` (置換) を開き、**正規表現モード** ( `.*` アイコン) をオン。
- **検索**: `^.*\[P\].*\n`
- **置換**: (空欄のまま「すべて置換」を実行)
- **効果**: 実績 `P` が含まれる行（＝完了した単発タスク）を丸ごと削除します。

---

## 3. 視認性とナビゲーション
情報の検索性とアクセスの速さを向上させます。

### 🌲 Todo Tree によるタスク抽出
VS Codeの `settings.json` に以下の設定を追加すると、`- [ ]` で始まる未完了タスクをサイドバーに集約できます。

```json
"todo-tree.general.tags": [
    "- [ ]"
],
"todo-tree.tree.showScanModeButton": false,
"todo-tree.highlights.threshold": 0,
"todo-tree.general.statusBar": "total"
```

### 🔍 セクションへの高速ジャンプ
- **操作**: `Ctrl + Shift + O` (Mac: `Cmd + Shift + O`)
- **効果**: ファイル内の見出し（# Daily, # Weekly 等）が一覧表示され、長いファイル内でも目的の場所に即座にジャンプできます。

### 🌓 エディタ分割 (Split View)
- **操作**: `Ctrl + \` (Mac: `Cmd + \`)
- **効果**: 左に `Stock.md`、右に `Daily.md` を並べ、コピペによるタスク転記をスムーズにします。

---

## 4. 運用の「儀式」 (Rituals)

| タイミング | 所要時間 | アクション |
| :--- | :--- | :--- |
| **朝 (Set)** | 5分 | `Daily.md` 生成、`Stock.md` からのタスク転記。 |
| **都度 (Log)** | 10秒 | ポモドーロ終了ごとに `p` → `P` 変換。 |
| **夕 (Sync)** | 5分 | 割り込みメモを `Stock.md` へ移記、翌日の見通しを確認。 |
| **週末 (Review)** | 15分 | 正規表現によるクリーンアップ、時間軸（Weekly等）の昇格。 |